# Multi-Agent System — Практика 3

Проект по дисциплине «Мультиагентные системы», выполненный на Java с использованием платформы **JADE**.

---

## 📦 Содержание проекта

Проект реализует третью практическую работу:

1. **Координатор через DF и одноранговая MAS (поиск маршрутов)**

Все агенты запускаются через общий файл `Main.java`.

---

## 🧪 Практика 3 — Основные задачи

### Задача 1 — Координатор и вычислители через DF

**Цель:**
- Координатор получает список агентов-вычислителей через DF, а не через аргументы.
- Периодически обновляет список доступных вычислителей.

**Логика работы:**

- **Вычислители (`CalculatorAgent`)**
    - Принимают `REQUEST` (язык `sum`) с содержимым `"A,B"`.
    - Считают сумму диапазона `A…B`.
    - Отправляют `CONFIRM` с результатом.

- **Координатор (`CoordinatorAgentDF`)**
    - Принимает `REQUEST` на сумму.
    - Обновляет список рабочих через DF каждые N секунд.
    - Разделяет диапазон задач между вычислителями.
    - Суммирует ответы и возвращает итог.

**Реализовано:**
- Автоматическая регистрация и поиск агентов в DF.
- Периодическое обновление списка вычислителей.
- Распределение задач и сбор результатов в `Map<String,Integer>`.
- Логирование всех шагов.

---

### Задача 2 — Одноранговая MAS (поиск маршрутов)

**Цель:**
- Смоделировать неориентированный граф.
- Каждый узел представлен агентом (`NodeAgent`).
- Соседи передаются агенту через аргументы.
- Любой агент может запросить маршрут до любого другого агента через `REQUEST`.

**Логика работы:**

- **NodeAgent**
    - Принимает `REQUEST` с языком `"route"`.
    - Содержимое: `"TargetNode;currentPath"`.
    - Добавляет себя в путь.
    - Если достигнут целевой узел — отправляет `CONFIRM` с маршрутом.
    - Если нельзя продолжить поиск — отправляет `DISCONFIRM`.
    - Пересылает запрос соседям, которых ещё не посещал.

**Реализовано:**
- Отправка запроса маршрута через GUI или тестовый агент.
- Логирование маршрута на каждом узле.
- Обработка результата: `CONFIRM` (маршрут найден) или `DISCONFIRM` (не найден).

---

## 🚀 Запуск проекта

### 1. Компиляция

Windows:

```powershell
javac -cp "lib/jade.jar" -d out src/main/java/*.java
```
### 2. Запуск

```powershell
java -cp "out;lib/jade.jar" Main
```

---


# 🏁 Проверка работоспособности

### Задача 1 — Координатор и вычислители

1. В RMA GUI видно агентов:

- calc1, calc2, calc3, coordinatorDF

2. Через Sniffer наблюдаем:

- отправку задач координатором

- вычисление сумм рабочими

- сбор итоговой суммы

### Задача 2 — Поиск маршрута

1. В RMA GUI видно узлы:

nodeA, nodeB, nodeC, nodeD

2. Пример отправки запроса маршрута из nodeA до nodeD:
- message: request
- language: route
- content: node_name
- receiver: node_name

3. После обработки:
- Если маршрут найден — CONFIRM с маршрутом
- Если маршрут недоступен — DISCONFIRM