# Multi-Agent System — Практика 1 & 2

Проект по дисциплине «Мультиагентные системы», выполненный на Java с использованием платформы **JADE**.

---

## 📦 Содержание проекта

Проект реализует две практические работы:

1. **Практика 1 — Основы JADE**
2. **Практика 2 — Агент-координатор и вычислители (распределённые вычисления)**

Все агенты запускаются через общий файл `Main.java`.

---

## 🧪 Практика 1 — Основные возможности JADE

### Цели:
- установить JADE
- запустить главный контейнер с GUI
- познакомиться с RMA
- запускать DummyAgent и SampleAgent
- передавать сообщения между агентами и наблюдать через Sniffer
- создать собственного агента (`HelloAgent`)
- добавить поведение, логирование, чтение сообщений
- запускать агентов через GUI и из Java-кода

### Реализовано:

- `HelloAgent.java` — выводит «Hello World», принимает сообщения, логирует коммуникативный акт, отправителя и содержимое
- `SampleAgent.java` — аналогично HelloAgent, используется как второй тестовый агент
- `ReceiverAgent.java` — пример простого агента-слушателя для тестирования обмена сообщениями
- Запуск через `Main.java` создаёт автоматически агенты:
    - `Alice` — HelloAgent
    - `bob` — SampleAgent

---

## 🧮 Практика 2 — Координатор и вычислители

### Агентная среда:

- 1 агент-координатор
- 3 агента-вычислителя

### Логика:

**Вычислители (`CalculatorAgent`):**
- принимают `REQUEST` (язык `sum`)
- содержимое: два числа `A,B`
- считают сумму диапазона `A…B`
- отправляют `CONFIRM` с результатом

**Координатор (`CoordinatorAgent`):**
- принимает `REQUEST: "a,b"`
- распределяет диапазон между вычислителями
- собирает их ответы
- суммирует результаты
- отправляет итоговое значение клиенту

### Реализовано:

- автоматическая регистрация агентов-вычислителей в DF
- обновление списка рабочих каждые 5 секунд
- распределение диапазона задач между рабочими
- сбор результатов в `Map<String,Integer>`
- отправка итоговой суммы обратно клиенту
- логирование всех шагов

---

## 🚀 Запуск проекта

### 1. Компиляция

Windows:
```powershell
javac -cp "lib/jade.jar" -d out src/main/java/*.java
```
### 2. Запуск

```powershell
java -cp "out;lib/jade.jar" Main
```

---


## 🏁 Проверка работоспособности

В RMA GUI видно агентов:

- Alice

- bob

- calc1

- calc2

- calc3

- coordinator

Через Sniffer можно наблюдать:

- как координатор отправляет задачи

- как вычислители отвечают

- как координатор собирает итог

Для теста можно отправить сообщение напрямую из GUI (к примеру — '1,100'):

Receiver: coordinator

Language: sum

Content: 5500

Координатор распределит диапазон между вычислителями и вернёт итоговую сумму